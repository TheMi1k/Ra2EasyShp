<Window x:Class="Ra2EasyShp.CreatePaletteConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ra2EasyShp"
        mc:Ignorable="d"
        Title="色盘保存选项" Height="600" Width="260" FontSize="15" ResizeMode="NoResize">
    <Grid>
        <StackPanel Margin="10 10 0 0">
            <StackPanel x:Name="StackPanel_Color" Orientation="Horizontal" Height="384" Width="224" HorizontalAlignment="Left">

            </StackPanel>

            <StackPanel Margin="0 5 0 0">
                <GroupBox Header="命名(为空保存为 色盘.pal)">
                    <StackPanel Margin="5 5 0 0">
                        <StackPanel Orientation="Horizontal">
                            <TextBox x:Name="TextBox_FileName" Width="120" HorizontalAlignment="Left"/>
                            <TextBlock Text=".pal" Margin="5 0 0 0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                            <CheckBox x:Name="CheckBox_MapTypeForName" Content="地图类型命名" Margin="0 0 0 0"/>
                            <Button Content="?" Height="20" Width="20" Margin="10 0 0 0" Click="Button_Tip_Click"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="30 20 0 0">
                <Button Content="取消" Width="60" FontSize="20" Click="Button_Cancel_Click"/>
                <Button Content="保存" Width="60" FontSize="20" Margin="40 0 0 0" Click="Button_Save_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
