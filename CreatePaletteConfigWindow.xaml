<Window x:Class="Ra2EasyShp.CreatePaletteConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ra2EasyShp"
        mc:Ignorable="d"
        d:Title="色盘保存选项" Title="{Binding LanguageDic[Title_SavePalette]}" Height="430" Width="200" FontSize="15" ResizeMode="NoResize">
    <Grid>
        <StackPanel Margin="10 10 0 0">
            <StackPanel x:Name="StackPanel_Color" Orientation="Horizontal" Height="320" Width="160" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0 0 0 0">

            </StackPanel>

            <StackPanel Margin="0 10 0 0" VerticalAlignment="Bottom">
                <StackPanel Orientation="Horizontal" Margin="0 0 0 0">
                    <Button d:Content="取消" Content="{Binding LanguageDic[Text_SavePalette_Cancel]}" MinWidth="60" FontSize="20" Click="Button_Cancel_Click"/>
                    <Button d:Content="保存" Content="{Binding LanguageDic[Text_SavePalette_Save]}" MinWidth="60" FontSize="20" Margin="30 0 0 0" Click="Button_Save_Click"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
